version: '3.1'

services:
  app:
    build: .
    restart: always
    secrets:
      - hackerrank-username
      - hackerrank-password

secrets:
  hackerrank-username:
    file: hackerrank-username
  hackerrank-password:
    file: hackerrank-password
